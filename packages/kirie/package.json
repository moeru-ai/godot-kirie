{
  "name": "@gd-kirie/kirie",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run generate:godot",
    "generate:godot": "pnpm run typecheck && tstogd convert --no-check",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "typescript-to-gdscript": "catalog:"
  }
}
