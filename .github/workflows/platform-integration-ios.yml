name: iOS Platform Integration

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios:
    name: iOS
    runs-on: macos-latest
    env:
      GODOT_VERSION: 4.7.2
      HOMEBREW_NO_AUTO_UPDATE: "1"
      HOMEBREW_NO_INSTALL_CLEANUP: "1"
      MISE_EXEC_AUTO_INSTALL: "false"
      MISE_NOT_FOUND_AUTO_INSTALL: "false"
      MISE_TASK_RUN_AUTO_INSTALL: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Node and pnpm
        uses: jdx/mise-action@v3
        with:
          install: true
          install_args: --locked node pnpm
          cache: true

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: ${{ env.GODOT_VERSION }}
          use-dotnet: true
          include-templates: true

      - name: Setup Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Get Xcode version
        id: xcode
        run: |
          xcodebuild -version
          echo "version=$(xcodebuild -version | tr ' \n' '--')" >> "$GITHUB_OUTPUT"

      - name: Install XcodeGen and SCons
        run: brew install xcodegen scons

      - name: Download and Prepare Godot Source Headers
        run: |
          rm -rf godot "${GODOT_VERSION}-stable.tar.gz" "godot-${GODOT_VERSION}-stable"
          curl -LO "https://github.com/godotengine/godot/archive/refs/tags/${GODOT_VERSION}-stable.tar.gz"
          tar -xzf "${GODOT_VERSION}-stable.tar.gz"
          mv "godot-${GODOT_VERSION}-stable" godot

          cd godot

          scons platform=macos target=editor \
            vulkan=no \
            core/version_generated.gen.h \
            core/disabled_classes.gen.h \
            core/extension/gdextension_interface.gen.h \
            core/object/gdvirtual.gen.h

      - name: Cache Godot iOS simulator template
        uses: actions/cache@v5
        with:
          path: godot/bin/libgodot.ios.template_debug.arm64.simulator.a
          key: ${{ runner.os }}-${{ runner.arch }}-godot-${{ env.GODOT_VERSION }}-${{ steps.xcode.outputs.version }}-ios-template_debug-arm64-simulator

      - name: Get pnpm store path
        id: pnpm-store
        run: echo "path=$(mise x -- pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-${{ runner.arch }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-pnpm-store-

      - name: Install pnpm dependencies
        run: mise x -- pnpm install --frozen-lockfile

      - name: Build iOS debug XCFramework
        run: mise run build:ios-debug-xcframework

      - name: Build integration App
        run: |
          mise run build:integration-ios
          du -sh dist/integration/ios_debug.app
          du -sh dist/integration/ios_debug.app/*

      - name: Select iOS Simulator
        timeout-minutes: 1
        run: |
          DEVICE_ID=$(xcrun simctl list devices available | grep -m 1 "iPhone 16" | awk -F '[()]' '{print $2}')
          echo "Selected device ID: $DEVICE_ID"
          echo "SIMULATOR_ID=$DEVICE_ID" >> "$GITHUB_ENV"

      - name: Boot iOS Simulator
        timeout-minutes: 1
        run: xcrun simctl boot "$SIMULATOR_ID"

      - name: Wait for iOS Simulator
        timeout-minutes: 4
        run: xcrun simctl bootstatus "$SIMULATOR_ID"

      - name: Run integration tests
        timeout-minutes: 15
        run: |
          mise run --skip-deps test:integration-ios -- ipc_round_trip_probe
          mise run --skip-deps test:integration-ios -- webview_lifecycle_probe
          mise run --skip-deps test:integration-ios -- res_asset_loading_probe

      - name: Diagnose iOS Simulator failure
        if: failure()
        timeout-minutes: 2
        continue-on-error: true
        run: |
          ps -axo pid,ppid,etime,state,comm | grep -E '(^ *PID|/simctl$|/com[.]apple[.]CoreSimulator[.]CoreSimulatorService$|/SimLaunchHost[.]arm64$)'
          /usr/bin/log show --last 5m --style compact --predicate '(process CONTAINS "CoreSimulator" OR subsystem BEGINSWITH "com.apple.CoreSimulator") AND (logType == "error" OR logType == "fault")' | tail -n 100
          /usr/bin/log show --last 5m --style compact --predicate 'process CONTAINS "CoreSimulator" OR subsystem BEGINSWITH "com.apple.CoreSimulator"' | tail -n 100

      - name: Check iOS Simulator state after failure
        if: failure()
        timeout-minutes: 1
        continue-on-error: true
        run: xcrun simctl list devices
